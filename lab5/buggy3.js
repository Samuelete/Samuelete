function gogogo() { // Bug found it here. It should have ; after gogogo()
    alert("Yay, it works!"); 
    // Bug found here It has '' instead of "
    // also the function alert() started with A instead of a
}